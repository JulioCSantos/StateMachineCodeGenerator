<UserControl x:Class="StateMachineCodeGenerator.Gui.FilesSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StateMachineCodeGenerator.Gui"
             xmlns:viewModels="clr-namespace:StateMachineCodeGenerator.ViewModels;assembly=StateMachineCodeGenerator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModels:FilesSelectionViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="200*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <GroupBox Header="EA exported XML file" >
                <TextBox  Width="500" Height="70" VerticalAlignment="Top"
                         TextWrapping="Wrap"
                         Text="{Binding FilePath}"  />
            </GroupBox>
            <GroupBox Header="Target path' Generated files" >
                <TextBox  Width="500" Height="70" VerticalAlignment="Top"
                          TextWrapping="Wrap"
                          Text="{Binding TargetPath}"  />
            </GroupBox>
        </StackPanel>

        <Button Grid.Row="2" Content="Start Parsing"
                Command="{Binding StartParsingCommand}"
                Width="80" Height="23" VerticalAlignment="Top" HorizontalAlignment="Center" />
    </Grid>
</UserControl>
